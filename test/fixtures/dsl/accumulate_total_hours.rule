rule "accumulate-total-hours" salience: 70 do
  when
    e: TimesheetEntry(employee_id: emp, start_at: s, end_at: f)
    guard hours_between(s, f) > 0
  then
    emit HoursTotal(employee_id: emp, hours: hours_between(s, f))
end
